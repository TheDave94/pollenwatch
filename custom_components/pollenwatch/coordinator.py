"""Data update coordinators for PollenWatch.

Stub — implemented in a later milestone. Design: one
``DataUpdateCoordinator`` per data source, plus a meta-coordinator that listens
to all source coordinators and computes the analytics entities.
"""

from __future__ import annotations
