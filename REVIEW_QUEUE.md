# Review queue

Time- or data-gated items to revisit later. Not bugs and not active scope —
things that need real-world data or elapsed time before they can be decided.

| Added | Revisit after | Item |
| --- | --- | --- |
| 2026-05-29 | ~4 weeks of live data (≈ 2026-06-26) | Evaluate whether the **60 min default update interval** is right. Lower risks stale data relative to how often the hourly CAMS data actually moves; higher wastes the freshness Open-Meteo provides. Decide from the live Graz instance's recorder history (how much do values change hour-to-hour vs. the polling cost). |
| 2026-05-29 | Conscious decision (no action) | **polleninformation.at API key is exposed-in-transcript; rotation consciously declined** — free / read-only / fair-use-rate-limited, trivial blast radius (worst case: someone burns the pollen quota until rate-limited; no cost, no sensitive access). Revisit only if polleninformation changes to paid, or the key is reused somewhere with real stakes. |
| 2026-05-29 | After 3b ships | **Open-Meteo 92-day backfill is re-fetched hourly** (for recent_percentile). Past data doesn't change, so this is wasteful payload. Consider fetching the backfill once/day and only the forecast hourly, or caching the historical daily peaks. |
| 2026-05-29 | After real dual-source data | **Divergence ">1 level apart" threshold** — confirm how often it fires once consensus/divergence ship; tune if too noisy/quiet. |
| 2026-05-29 | After real 3-source data | **3-source consensus `{1,1,2}` wart (interim, documented).** With ≥3 sources, take-the-higher lets a lone escalating outlier pull consensus up (e.g. {1,1,2}→high) while divergence stays **off** (spread is only 1), so a minority high reading presents as confident consensus — the opposite of what the consensus/divergence pair is for. Neither pure take-higher nor mode is right; the honest fix is a **divergence-semantics change** (divergence on when sources don't *unanimously* agree even within spread 1), which interacts with the 2-source case and wants a fresh-head redesign. Observe-then-tune; theoretical until a 3rd source is live (DWD doesn't cover Graz). |
| 2026-05-29 | Only if tradeoffs change | **Verbatim UPI 4/5 `indexDescription` not capturable from Google's static docs.** Re-check confirmed: Google's `/forecast` guide publishes example responses with `indexDescription` for UPI 0–2 only; UPI 4/5 don't appear anywhere on Google's static doc pages — they ship only dynamically in the API response. Recording them statically would need either a parser patch to surface `indexDescription` onto entity attrs (declined as over-broad for a footnote) or a paid query at a high-pollen location. Mapping keys on the documented category *name* (published for all six), not the prose, so this does not gate the logic. |
| 2026-05-29 | After throwaway watch ends | **Throwaway Google API key was pasted into transcript** during Sub-Step B setup, despite the "keys never in chat" protocol (the `!`-prefix file-write didn't land; user fell back to paste). The key is Pollen-API-restricted with a ~10 req/day Console cap, so dollar risk is bounded (overrun → HTTP 429, not billing), but the credential surface is now in chat history. **Action when the throwaway watch concludes:** revoke the throwaway key in the Cloud Console. (The live-HA Google key is a separate, unleaked key — not affected.) |
| 2026-05-31 | Next threshold-provenance pass | **ash onset refinement (held at 10/100 in v2.2).** The 2021 Aerobiologia systematic review documents ~18 grains/m³ onset for ash — the *strongest single per-species evidence* in the 24-species set — but the refinement was OUT OF SCOPE for v2.2's approved 6-species change list (ragweed/olive/birch/alder/hazel/mugwort). Cited basis: `analytics.py` ash row comment + `threshold-provenance-review-FINAL.md`. Wire `(18, 100)` next pass. |
| 2026-05-31 | When a source emits alternaria in spores/m³ | **alternaria spore threshold (cited but not wired).** Best-cited threshold in the entire 24-species set per the FINAL review: ~80–100 spores/m³ first symptoms (Rapiejko 2007; Ricci 1995; PMC4473279 European multi-city); modern refinement Alt a 1 ~20.7 pg/m³ (Thermo m229). NOT in `analytics._THRESHOLDS` because alternaria routes via the polleninformation 0–4 index (`collapse_index`) path, never `bucket_level` — adding an entry now would be dead code. Wire `(80, 100)` in `_THRESHOLDS` IF a future source ever emits alternaria as spores/m³. See `analytics.py` alternaria comment. |
| 2026-05-31 | When Fraunhofer UBAMBI ragweed arm publishes | **ragweed UBAMBI revisit.** v2.2 set ragweed to `(5, 20)` from published field data (PMC5357339 <20, sensitive 1–5; PMC2868868; Nature s41598-022-20069-y Milan 4-severity model). The Fraunhofer UBAMBI controlled-chamber trial (NCT05346718) completed Dec 2024 — **birch arm published May 2025**, **ragweed arm unpublished as of mid-2026**. When the ragweed arm publishes it will likely be the authoritative threshold per the FINAL review's "watch UBAMBI" note. Revisit the ragweed bracket then. |
| 2026-05-31 | When Options-flow harness coverage is built | **Extend `cleanroom-pretag` to drive the Options flow** (add/remove species → assert orphan-prune), covering the manual `§11` steps 5–7 now documented in `cleanroom/README.md` ("Manual checklist the harness doesn't cover"). Once automated, the manual interactive checklist becomes redundant and can collapse back into the Gates A–D output. Not started — captured so the gap is tracked, not acted on. |
| 2026-05-31 | Throwaway teardown / housekeeping | **Revoke throwaway-scoped credentials.** Two throwaway-only credentials accumulated: (a) the **throwaway LLAT minted 2026-05-31** for v2.2.0 deploy verification (90-day token on `throwaway-pollenwatch`, port 8124, refresh-token id `8c9d62070fb64ba9b1145d4a236e93a4`, name `throwaway-phase1`); (b) the **throwaway Google API key** already queued above. Both are throwaway-scoped — revoke when the dev box is next torn down or refreshed. |
